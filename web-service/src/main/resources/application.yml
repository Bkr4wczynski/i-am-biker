server:
  port: 8000
  servlet:
    context-path: /web

spring:
  application:
    name: web-service
  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka