server:
  port: 8000
  servlet:
    context-path: /web

jwt:
  secret: 9d5c3687c5dfa48ae9a7799eabf77d3c0de017797d75ed7d82e852ac358a6500
  expiration: 360000

spring:
  application:
    name: web-service
  config:
    import: optional:configserver:http://localhost:8888
  datasource:
    url: jdbc:mysql://localhost:3306/i_am_biker_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Europe/Warsaw
    username: biker
    password: vDmpgfi68FTefb2d
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

resilience4j:
  circuitbreaker:
    instances:
      defaultCircuitBreaker:
        registerHealthIndicator: true
        slidingWindowSize: 5
        minimumNumberOfCalls: 3
        failureRateThreshold: 50
        waitDurationInOpenState: 5s

  retry:
    instances:
      defaultRetry:
        maxAttempts: 5
        waitDuration: 1s

  ratelimiter:
    instances:
      defaultRateLimiter:
        limitsForPeriod: 10
        limitRefreshPeriod: 1s
        timeoutDuration: 0

  timelimiter:
    instances:
      defaultTimeLimiter:
        timeoutDuration: 5s
        cancelRunningFuture: true